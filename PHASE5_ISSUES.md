# Phase 5 问题修复清单

## 优先级 P0（立即修复）

### 1. 清理编译警告 ⏳

**问题描述**:
- 6 个编译警告（未使用的导入和方法）
- 增加编译时间和二进制大小

**修复方案**:
```bash
# 自动修复
cargo fix --lib -p fragment-vocab

# 或手动删除以下内容：
# 1. src/commands.rs:2 - 删除 models::SrsCard 导入
# 2. src/db/models.rs:43 - 删除 AppState 结构体（如果确实不需要）
# 3. 删除未使用的方法或添加 #[allow(dead_code)]
```

**预计工作量**: 15 分钟

---

### 2. 分析历史崩溃日志 ⏳

**问题描述**:
- 发现崩溃报告: `fragment-vocab-2026-03-12-032637.ips`
- 时间: 03:26:37（测试前）

**修复方案**:
1. 详细分析崩溃日志
2. 识别崩溃原因
3. 添加相应的错误处理
4. 添加更多日志记录点

**预计工作量**: 1-2 小时

---

## 优先级 P1（重要改进）

### 3. 内存占用优化 ⏳

**问题描述**:
- 当前内存占用: 126 MB
- 目标: < 50 MB
- 差距: 76 MB

**分析**:
- Tauri + WebView 基础开销: ~80MB
- Rust 运行时: ~20MB
- 前端资源: ~20MB
- Debug build 未优化

**修复方案**:

**方案 A: Release 构建优化**（推荐）
```bash
# 1. 构建 release 版本
npm run tauri build

# 2. 测试内存占用
# 预计减少 30-40% (降至 75-88 MB)
```

**方案 B: 前端资源优化**
- 压缩 CSS/JS
- 移除未使用的依赖
- 使用更轻量的字体
- 预计减少 5-10 MB

**方案 C: 技术栈调整**（如果必须 < 50MB）
- 考虑使用原生 UI（Swift/Objective-C）
- 或使用更轻量的框架（如 Iced）
- 预计工作量: 1-2 周

**建议**: 先尝试方案 A，如果仍不满足再考虑方案 B 和 C

**预计工作量**: 
- 方案 A: 30 分钟
- 方案 B: 2-4 小时
- 方案 C: 1-2 周

---

### 4. 添加日志系统 ⏳

**问题描述**:
- 当前只有 println! 输出
- 缺少结构化日志
- 难以排查问题

**修复方案**:
```toml
# Cargo.toml
[dependencies]
tracing = "0.1"
tracing-subscriber = "0.3"
tracing-appender = "0.2"
```

```rust
// 初始化日志
use tracing_subscriber::{layer::SubscriberExt, util::SubscriberInitExt};

fn setup_logging() {
    let file_appender = tracing_appender::rolling::daily(
        app_data_dir.join("logs"),
        "fragment-vocab.log"
    );
    
    tracing_subscriber::registry()
        .with(tracing_subscriber::fmt::layer().with_writer(file_appender))
        .with(tracing_subscriber::fmt::layer().with_writer(std::io::stdout))
        .init();
}
```

**预计工作量**: 1-2 小时

---

### 5. 完成打包测试 ⏳

**任务清单**:
- [ ] 构建 DMG: `npm run tauri build`
- [ ] 安装测试
- [ ] 开机启动验证
- [ ] 卸载测试
- [ ] 签名和公证（如需发布）

**预计工作量**: 1-2 小时

---

### 6. GUI 手动测试 ⏳

**测试清单**:
- [ ] 等待 90 秒空闲，验证自动弹卡
- [ ] 测试快捷键 Cmd+Shift+K/J/Esc
- [ ] 测试单词卡展示和动画
- [ ] 测试托盘菜单所有功能
- [ ] 测试统计页面
- [ ] 长时间运行稳定性（1-2 天）

**预计工作量**: 2-4 小时（分散在 1-2 天）

---

## 优先级 P2（可选优化）

### 7. 代码重构 ⏳

**改进点**:
- 删除未使用的方法或添加 `#[cfg(test)]`
- 统一错误处理（使用 `anyhow` 或 `thiserror`）
- 添加更多文档注释
- 提取魔法数字为常量

**预计工作量**: 2-4 小时

---

### 8. 增强测试覆盖 ⏳

**改进点**:
- 添加更多边界情况测试
- 添加集成测试
- 添加性能基准测试
- 测试覆盖率报告

**预计工作量**: 4-8 小时

---

### 9. 用户体验优化 ⏳

**改进点**:
- 添加首次启动引导
- 优化单词卡动画
- 添加音效（可选）
- 支持自定义主题
- 支持导入自定义词库

**预计工作量**: 1-2 周

---

## 修复顺序建议

### 第一阶段（发布前必须完成）
1. 清理编译警告（15 分钟）
2. 分析崩溃日志（1-2 小时）
3. Release 构建测试（30 分钟）
4. 打包测试（1-2 小时）

**总计**: 3-4 小时

### 第二阶段（发布后优化）
1. 添加日志系统（1-2 小时）
2. GUI 手动测试（2-4 小时）
3. 内存优化（如需要）（2-4 小时）

**总计**: 5-10 小时

### 第三阶段（长期优化）
1. 代码重构（2-4 小时）
2. 增强测试覆盖（4-8 小时）
3. 用户体验优化（1-2 周）

---

## 当前状态总结

✅ **已完成**:
- 核心功能实现
- 单元测试（47/47 通过）
- 集成测试
- 基础性能测试

⏳ **待完成**:
- 编译警告清理
- 崩溃日志分析
- 打包测试
- GUI 手动测试

⚠️ **需优化**:
- 内存占用
- 日志系统
- 错误处理

---

## 发布建议

**v0.1.0 发布条件**:
- ✅ 核心功能完成
- ✅ 单元测试通过
- ⏳ 编译警告清理
- ⏳ 打包测试通过
- ⏳ 基础 GUI 测试通过

**建议**: 完成第一阶段修复后即可发布 v0.1.0，第二、三阶段作为后续版本优化。
